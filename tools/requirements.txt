# Core language and typing utilities
typing-extensions

# Environment variable loading
python-dotenv

# HTTP requests
requests

# LangChain core and ecosystem - pinned to compatible versions
langchain<1.0
langchain_ibm<1.0
langchain_core<=0.3.63

# LangGraph - pinned to versions compatible with langchain-core<1.0
langgraph<1.0
langgraph-prebuilt<1.0

elasticsearch==8.19.1

jinja2

ibm-cos-sdk

paramiko
structlog